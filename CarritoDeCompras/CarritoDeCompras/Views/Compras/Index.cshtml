@model IEnumerable<CarritoDeCompras.Models.Compra>

@{
    ViewData["Title"] = "Index";
}

<h1>Lista de compras</h1>

<p>
    @if (User.Identity.IsAuthenticated && User.FindFirstValue(ClaimTypes.Role) == "Empleado")
    {
        <a asp-action="Create">Crear nueva compra</a>
    }

</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Cliente)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Carrito)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Total)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
 @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Cliente.Apellido)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Carrito.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Total)
            </td>
            <td>
                @if (User.Identity.IsAuthenticated && User.FindFirstValue(ClaimTypes.Role) == "Empleado")
                {
                    <a asp-action="Edit" asp-route-id="@item.Id">Editar</a> 
                 }
                
                <a asp-action="Details" asp-route-id="@item.Id">Detalles</a> |
                @if (User.Identity.IsAuthenticated && User.FindFirstValue(ClaimTypes.Role) == "Empleado")
                {
                    <a asp-action="Delete" asp-route-id="@item.Id">Eliminar</a>
                }

            </td>
        </tr>
}
    </tbody>
</table>
