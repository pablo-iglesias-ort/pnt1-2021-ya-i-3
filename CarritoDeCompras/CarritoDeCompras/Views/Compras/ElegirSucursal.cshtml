@model CarritoDeCompras.Models.Compra

@{
    ViewData["Title"] = "ElegirSucursal";
}

<h1>Elegir sucursal para finalizar la compra</h1>


<!----- <table class="table table-hover">
    <thead class="thead-light">
        <tr>
            <th>
               Html.DisplayNameFor(model => model.Sucursal.Nombre)
            </th>
            <th>
                Html.DisplayNameFor(model => model.Sucursal.Descripcion)
            </th>
            <th>
                Html.DisplayNameFor(model => model.Sucursal.Telefono)
            </th>
            <th>
            </th>
        </tr>
    </thead>
    <tbody>
        foreach (var item in Model)
        {
            <tr>
                <td>
                    Html.DisplayFor(modelItem => item.Sucursal.Nombre)
                </td>
                <td>
                    Html.DisplayFor(modelItem => item.Sucursal.Descripcion)
                </td>
                <td>
                    $Html.DisplayFor(modelItem => item.Sucursal.Telefono)
                </td>
                <td>

                    <a class="btn btn-outline-warning btn-sm" asp-action="Details">Retirar en esta sucursal</a>
                 </td>
            </tr>
        }

    </tbody>
</table>
    >
